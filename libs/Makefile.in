# @configure_input@
# Bundled libraries removed — system libraries are now required.
# See docs/INSTALL-source.md for dependency installation instructions.

all:

distclean: clean

clean:

install: all
uninstall:
